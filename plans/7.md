# Plan for #7: Add readiness and liveness probes, run two replicas

## Goal
Configure Kubernetes to run two application replicas and expose readiness and
liveness checks against `/health` on the HTTP container port. Application
routes, metrics configuration, and the existing HTTP Service behavior remain
unchanged.

## Dependencies (confirmed / plausible / open), each with change: yes | no
- **confirmed — change: yes:** `k8s/deployment.yaml` has `replicas: 1` and no
  probes; set two replicas and add HTTP probes targeting port `http`.
- **confirmed — change: no:** `src/app.ts` already serves `/health` with HTTP
  200 and `{ status: "ok" }`.
- **confirmed — change: no:** `k8s/service.yaml` targets the named `http` port,
  which remains the readiness/liveness port.
- **confirmed — change: no:** `k8s/configmap.yaml`, `src/config.ts`, tests,
  `public/`, and README environment documentation need no changes.
- **plausible — change: yes:** `README.md` may need deployment documentation
  for two replicas and probes; verify whether its pipeline/deployment section
  already describes these operational guarantees.

## Tests
- Run `npm run verify` to preserve application behavior.
- Validate the deployment through the pipeline's rollout, stability, smoke,
  and browser checks; confirm both pods become Ready.

## Environment contract
No variables are added or changed. The existing `src/config.ts`,
`k8s/configmap.yaml`, and README environment table remain synchronized.

## Risks
Probe paths and ports must match the existing `/health` route and named HTTP
port. Incorrect probe timing or replica configuration could block rollout or
cause stability failures; the pipeline's rollout, stability, smoke, and browser
gates are the key validation.

## Open questions
None.

## Decisions
Use HTTP GET probes on `/health` against the named `http` port with Kubernetes
defaults for timing, keeping the change minimal and consistent with the
existing health endpoint.
